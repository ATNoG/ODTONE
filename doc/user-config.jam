#==============================================================================
# You must add these definitions to your user-config.jam (must be located in
# your HOME. Replace 'path-to-boost' with the proper paths.
#==============================================================================

project user-config
	: requirements <include>$(BOOST_INC)
	;

using boost
	: 1.42
	: <root>path-to-boost
	;

